# Roder Homebrew Tap

Install the latest published Roder CLI:

```sh
brew tap RoderAI/tap
brew install roder
```

You can also install without adding the tap first:

```sh
brew install RoderAI/tap/roder
```

This tap builds `roder` from the published crates.io source archive.
